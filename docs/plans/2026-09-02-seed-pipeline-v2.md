# 시드 파이프라인 v2 — 보편 추출 × 디깅 보강 2층 구조

2026-09-02 확정 (딥리서치 3갈래 후 PO 결정: "보편적으로 뽑고, 디깅 글로 보강"). 이 문서만 읽고 다른 에이전트가 구현을 이어받을 수 있어야 한다.

## 조사 결론 요약 (2026-09-02 딥리서치, 근거는 하단)

1. **네이버 지역정보는 어떤 필드든 저장 불가** — 오픈API 약관 7.3③이 "지역정보 DB화"를 금지 예시로 명시, 완화 답변·유료 경로 없음. 발굴 파이프라인에서 제외
2. **카카오 로컬 API는 장소ID·place_url만 저장 허용** — 데브톡 운영진 반복 승인. "공공데이터 자체 DB에 ID·URL만 배치 매칭" 시나리오가 명시 승인됨(스레드 151335). 좌표·주소·카테고리·상호명은 불허(상호명은 답변 상충 — 비권장). 임시 저장도 저장
3. **업종 재현율은 공공데이터로 해결** — 소진공 상가(상권)정보 업종 소분류 `I20303 "일식 면 요리"` 실존(실데이터 확인), WGS84 좌표 직제공, 라이선스 "제한 없음". 상호에 '라멘' 없는 전문점을 잡는다 (소바·우동 혼입은 제외어+검수로)
4. **좌표 보정 합법 경로** — juso.go.kr 좌표제공 API(출입구 좌표, EPSG:5179): 지도사 지오코딩 저장 금지에 저촉되지 않는 유일 확인 경로 (사업자 신청·승인 필요)
5. **판례**: 크롤링은 형사 무죄여도 민사에서 짐(여기어때 성과도용 10억, 사람인 DB권 2.5억) — 지도사·경쟁 서비스 크롤링 배제 재확인
6. **디깅 소스는 발굴 단서로 적법** — 상호·주소·존재 같은 사실은 저작권 비보호. 단 목록의 선정·배열 통째 이식(편집저작물)과 서술 복사는 금지
7. OSM 기각 (서울 라멘 POI 37건 + ODbL share-alike 오염 리스크)

## 설계 — 2층 구조

### 1층: 보편 추출 (자동, 넓게) — 2026-09-02 실측 반영판

**실측 (상가정보 서울 202606 CSV, 다운로드는 data.go.kr 15083033에서 비로그인 가능 — 전국 zip 352MB):**
- 서울 I20303 = 603건, 상호 키워드 매칭(전 업종) = 238건, 교집합 190건
- **I20303은 주력 축이 될 수 없음**: 키워드 매칭 라멘집의 20%(48건)가 I20303 밖(일식 회/초밥·분식·베트남식 등 오분류 — 멘야코노하도 "일식 회/초밥"). 업종 분류 자체가 부정확
- **I20303은 보조 발굴로 유효**: 키워드 미포함 413건에 제외어(우동·소바·돈가스·초밥·카레·규동·덮밥 등) 적용 시 **34건**으로 압축되고, 그 안에 키워드가 못 잡는 라멘 체인 실재 — 아오리의행방불명·산쪼메·멘무샤·히노아지·멘부리·진가와
- 좌표: I20303 603건 전부 유효 WGS84 (불량 0)

**확정 설계**: **키워드 매칭(주력, 상가정보·LOCALDATA 양쪽에 적용) ∪ (I20303 ∩ 제외어 통과)(보조 발굴)** — I20303 전량 합집합은 기각(우동·소바 413건 검수 부담)
- **조인**: 상호 정규화+주소로 두 소스 매칭 — LOCALDATA가 폐업·인허가 상태 담당, 상가정보가 좌표·발굴 담당. 한쪽에만 있는 행도 후보 유지(출처 표시). 서울 후보 규모 ~270건(중복 제거 전) — 시트 검수 가능
- **좌표**: 상가정보 WGS84 기본 → LOCALDATA(EPSG:5174 변환, 기존 코드) 좌표와 대조 → 150m 초과 시 `coord: mismatch` 플래그 (juso 보정은 백로그)
- 산출: `data/out/candidates.tsv` — 컬럼 추가: 출처(localdata/상가/양쪽/I20303발굴), 좌표 플래그
- **조인 실측 완료 (2026-09-02, 서울시 인허가 CSV 53.7만 행)**: 인허가 키워드 매칭 571건 중 영업 중 279건(폐업·취소 292건 필터 — 인허가 조인의 핵심 가치 실증), 영업 중 좌표 누락 0. 상가정보 키워드 238건과 상호 완전일치 조인 72% — 미매칭 28%는 지점명·수식어 표기 차이("쇼부" vs "쇼부일본라멘강남고속터미널1호점")가 주원인이므로 구현 시 **정규화 부분 매칭 + 행정동 보조 키** 필요. 인허가(279) &gt; 상가(238)로 어느 쪽도 단독 완전하지 않음 — 합집합 재확인

### 데이터 원본 확보 상태 (사용자 다운로드 불필요)

- 상가정보 전국 zip(352MB): `.playwright-mcp/소상공인시장진흥공단-상가-상권-정보-20260630.zip` — data.go.kr 15083033에서 비로그인 다운로드 가능
- 서울 인허가 CSV(223MB, **EUC-KR**, LOCALDATA 동일 스키마): `.playwright-mcp/서울시-일반음식점-인허가-정보.csv` — 서울열린데이터광장 OA-16094 "내려받기(CSV)" 비로그인 가능. localdata.go.kr 본 사이트·file 직링크는 접속 장애/403이라 이 경로가 안정적
- 둘 다 gitignore 경로(임시) — 파이프라인 입력은 인자로 경로 전달(현행 방식 유지)

### 2층: 디깅 보강 (수동, 깊게 — "덮어쓰기"의 정확한 의미)

- **디깅 소스**: Hypebeast 서울 라멘 67선, 디시 일본 라멘 마이너 갤러리·라멘 미니 갤러리 정리글("가본 곳 정리"·"오픈 리스트"), 매장 공식 인스타
- **동작**: 디깅 글에서 가게의 **존재를 인지** → 1층 후보 풀에 있으면 verification 근거·태깅 초안(장르·계보)을 시트에 입력, 없으면 신규 후보로 추가(사실 정보는 LOCALDATA·공식 채널에서 확인해 기입)
- **저작권 경계 (준수 필수)**: 글의 서술·평가 문장 복사 금지 / 특정 목록의 선정·배열 통째 이식 금지 / 태깅은 글을 참고한 운영자의 자체 판단으로 기입. 시트에 참고 링크 컬럼(내부 근거용, 미게재)
- 이 층은 18.데이터전략 v1 "파트너 온라인 조사 태깅"의 공식화·확장 — 새 프로세스가 아니라 발굴 단서 소스를 명시한 것

### (선택) 카카오 아웃링크 매칭

- 시드 확정 후 매장별 카카오 장소ID·place_url을 **운영자 수동 배치**로 시트에 입력 (저장 허용 필드만 — 승인된 경계). 상세 페이지 "카카오맵에서 보기" 아웃바운드 링크로 활용. 자동 API 배치 매칭은 하지 않는다(검색 API 응답의 다른 필드가 스치는 구조 자체를 회피)

## 기존 코드 포인터

| 파일 | 역할 |
| --- | --- |
| `scripts/seed-candidates.ts` | 진입점 — 인자에 상가정보 CSV 추가, 합집합·조인 로직 삽입 지점 |
| `scripts/lib/localdata.ts` | `RAMEN_KEYWORDS`·`isRamenCandidate`·`parseLocalData`·EPSG:5174→WGS84 변환(proj4) — 변환 코드 재사용 |
| `scripts/lib/localdata.test.ts` | 파서 테스트 문법 — 신규 상가정보 파서도 동일하게 `*.test.ts` co-location 필수 |
| `scripts/lib/sheet-parser.ts` + `scripts/sync-sheet.ts` | 시트 → shops.json 반영 (2층 결과 수용 — 참고 링크 컬럼 추가 시 파서 보강) |
| `scripts/diff-localdata.ts` | 신규·폐업 감지 — 변동분 API 전환(월 1회, 전월 24일 소급 한계)은 후속 검토 |
| `docs/planning/18.데이터전략.md` | v1 시드 절 개정 대상 (아래) |

## 데이터 소스 접근 정보

- 상가정보 분기 CSV: data.go.kr 15083033 (업종코드 파일 15067631) — 사용자가 다운로드, 서울분
- LOCALDATA 일반음식점: localdata.go.kr CSV (현행)
- 상가정보 좌표는 WGS84 십진수 — 변환 불필요

## 검증

- 신규 파서·조인·필터 순수 함수 Vitest 필수 (`scripts/lib/*.test.ts`)
- 실 CSV로 드라이런: 후보 수·출처 분포·좌표 mismatch 비율 출력 → I20303 단독 발견분(키워드 필터가 놓치던 가게) 샘플 10곳을 수동 대조해 재현율 개선 실증
- `npm test`

## 작업 절차

1. `feat/seed-v2` 브랜치 (main에서 — **선행: feat/record-photos 검수·머지 후**)
2. 상가정보 파서 → 조인·합집합 → candidates.tsv 확장 → 18 문서 개정 → 드라이런 검증
3. 시트 태깅(2층 디깅 보강)은 코드 머지 후 운영 작업 — 사용자와 파트너가 진행

## planning/18 개정안 (구현 브랜치에 포함)

- v1 시드 절: "LOCALDATA 자동 추출" → "LOCALDATA ∪ 상가정보 I20303 2소스 교차(2026-09-02)" + 디깅 보강 층·저작권 경계 3원칙 기록
- 대원칙에 추가: 지도사 API 저장 경계(네이버 전면 불가·카카오 ID/URL만) 조사 결과 각주

## 백로그

- juso.go.kr 좌표제공 API 승인 신청 → 좌표 보정 자동화
- LOCALDATA 변동분 API 월간 동기화 (현행: 수동 CSV diff)
- 식품안전나라 위생등급 조인 (인허가번호 기반, 선택 표시)

## 조사 출처 (딥리서치 2026-09-02)

- 카카오 데브톡 운영진 답변: 151335(공공DB+ID/URL 매칭 승인)·151265·151271·150642(좌표 불허·유료 플랜 없음)·150668(상호명 불허)
- 네이버 오픈API 약관 7.3③ (지역정보 DB화 금지 명시), NCP 약관 (유료도 DB화 금지)
- data.go.kr 15083033(상가정보)·15067631(업종코드) — I20303 실측 확인
- 판례: 야놀자 v 여기어때 (대법 2021도1533 형사 무죄 / 민사 성과도용 10억), 잡코리아 v 사람인 (DB제작자권 2.5억)
- Hypebeast 서울 라멘 67선, 디시 donkotsu·goodramen 갤러리 정리글
